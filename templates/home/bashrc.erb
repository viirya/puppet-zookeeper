# Set Users path
export PATH=$PATH:/bin:<%= scope.lookupvar('zookeeper::params::java_home') %>/bin:<%= scope.lookupvar('zookeeper::params::zookeeper_base') %>/zookeeper/bin

# Set JAVA_HOME (we will also configure JAVA_HOME directly for Hadoop later on)
export JAVA_HOME=<%= scope.lookupvar('zookeeper::params::java_home') %>

